<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href={{ path('home') }}>Solar</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item inicioTab">
                <a class="nav-item nav-link inicioTab" href={{ path('home') }}><i class="fas fa-home"></i> Inicio </a>
            </li>
            <li class="nav-item granjaTab">
                <a class="nav-item nav-link " href="{{ path('granjas') }}"><i class="fas fa-solar-panel"></i>
                    Granjas</a>
            </li>
            <li class="nav-item contactoTab">
                <a class="nav-item nav-link " href="{{ path('contacto') }}"><i class="far fa-envelope"></i> Contacto</a>
            </li>
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <li class="nav-item energiaTab">
                    <a class="nav-item nav-link energiaTab" href="#" aria-disabled="true"><i class="fas fa-bolt"></i>
                        Energía</a>
                </li>
                <li class="nav-item localesTab">
                    <a class="nav-item nav-link localesTab" href="#" aria-disabled="true"><i
                                class="fas fa-building"></i> Locales</a>
                </li>
                {% if is_granted('ROLE_SUPER_ADMIN') %}
                    <li class="nav-item energiaTab">
                        <a class="nav-item nav-link energiaTab" href="#" aria-disabled="true"><i
                                    class="fas fa-users-cog"></i> Gerencia</a>
                    <li class="nav-item localesTab">
                {% endif %}
                <li class="nav-item dropdown userTab">
                    <a class="nav-item nav-link dropdown-toggle userTab" href="#" id="navbarDropdown" role="button"
                       data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" aria-disabled="true">
                        <i class="fas fa-user"></i> {{ user.username }}
                    </a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path('fos_user_security_logout') }}"> Logout</a>
                    </div>
                </li>

            {% else %}
                <a class="nav-item nav-link pull-right loginTab" href="{{ path('fos_user_security_login') }}"
                   aria-disabled="true"><i class="fas fa-user"></i> Login</a>
            {% endif %}
        </ul>
    </div>
</nav>